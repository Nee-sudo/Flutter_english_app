# TODO

- [x] Fix backend CORS to allow Flutter web origin (http://localhost:53144) by updating FRONTEND_URL handling in `backend/src/server.js`.
- [ ] Restart backend and verify `/api/tenses` responds with `Access-Control-Allow-Origin`.
- [ ] Retry Flutter web request and confirm the CORS error is gone.


