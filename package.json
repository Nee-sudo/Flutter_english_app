{
  "name": "language-stories-web",
  "private": true,
  "version": "1.0.0",
  "description": "Flutter web build entrypoint (Vercel / Render static)",
  "scripts": {
    "build": "bash scripts/vercel-build.sh"
  }
}
